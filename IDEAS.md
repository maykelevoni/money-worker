# IDEAS — parking lot

> Shiny ideas land here so they don't hijack the current work. Nothing here is committed.
> Promote an idea to STATE/DECISIONS only when the current open loops are closed (see RULES #2).

## Product / "help other people too"
- Multi-tenant version: "AI affiliate-funnel autopilot that tells a solo creator the single next
  thing to do." The on-rails / next-action layer is the differentiator, not video generation.
  Needs: accounts, billing, per-user API keys + cost handling. PARKED until first $1 earned.

## UX polish (after first lead)
- Empty states with "do this first" guidance instead of blank tables.
- Real offer-click tracking so Analytics `clicked`/`converted` stop reading 0.
- Replace emoji icons + de-dev the sidebar (hide Django admin) for a more "product" feel.

## Growth / content
- (add hooks, video angles, offers to test here)
